import React from 'react';

// defining the component props
interface Props {
  selectedBus: StringConstructor
}

const SvgMap: React.FC<Props> = ({ selectedBus }) => {
  return (
    <>
    </>
  );
}

export default SvgMap;
